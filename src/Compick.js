import React from "react";






  Compick = (opp) => {
    [Math.floor(Math.random() * ['rock', 'paper', 'scissors'].length)];
    const {compChoice} = this.state;
    this.setstate({ compChoice: opp, })
  }
